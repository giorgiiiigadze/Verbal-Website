# The site's public origin. Feeds canonical URLs, Open Graph tags, sitemap.xml
# and robots.txt. No trailing slash.
#
# Unset, everything falls back to the placeholder in content/site.ts. Set this
# before launch, and set it in Vercel's project settings for production.
NEXT_PUBLIC_SITE_URL=https://verbal.app
